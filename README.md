Here are the steps and commands used in cmd.

cd documents/PLPBasicGitAssignments
git init 
git remote add origin <Git Hub repository url>
type nul > hello.txt
git add .
git commit -m "....."
git push --set upstream origin master
